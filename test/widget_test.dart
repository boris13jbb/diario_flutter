import 'package:flutter_test/flutter_test.dart';

/// Smoke test sin inicializar Firebase (el app completo requiere opciones reales).
void main() {
  test('proyecto de pruebas activo', () {
    expect(2 + 2, 4);
  });
}
